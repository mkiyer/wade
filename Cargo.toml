[package]
name = "wade-kernel"
version = "0.1.0"
edition = "2021"
description = "Permutation-loop kernel for WADE (Wasserstein Area Differential Expression)"
license = "GPL-3.0-only"   # matches LICENSE and the README; was wrongly MIT

[lib]
name = "_kernel"
path = "rust/src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
# abi3-py310 matches requires-python: one wheel per platform works on
# every supported Python, instead of one per platform per minor version.
pyo3 = { version = "0.27", features = ["extension-module", "abi3-py310"] }
numpy = "0.27"
rayon = "1"
rand = "0.9"
rand_distr = "0.5"
rand_pcg = "0.9"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
